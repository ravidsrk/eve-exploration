# Live run evidence — 2026-06-19 (ship)

Lab keys used locally only (not committed). GitHub secrets configured via `gh secret set`.

## Production deploys

| Agent | URL | Smoke |
| --- | --- | --- |
| A06 | https://eve-incident-commander.vercel.app | health 200, session 202 |
| A04 | https://eve-support-triage.vercel.app | health 200, session 202 |

## Structure & builds

| Suite | Result |
| --- | --- |
| `npm test` | PASS (50 catalog agents, 64 evals) |
| `npm run smoke:production:build` | 10/10 PASS |

## Live catalog evals (lab / OpenRouter)

| Suite | Result |
| --- | --- |
| `npm run eval:flagship` | 5/5 PASS |
| `npm run eval:s-tier` | 5/5 PASS |
| `npm run eval:a-tier` | PASS |

## Reference (`validate:reference`)

**9/10 fixtures PASS** in full run. Remaining flake: `agent-tools` (`dynamic-tools/nested` — `fetch failed` / socket hang-up when 14 evals run against one dev server).

Mitigations shipped in this commit:

- `npm run cleanup:superserve` — free 30-sandbox quota
- Serial evals for `agent-tools` and `agent-tools-sandbox` (`maxConcurrency: 1`)
- SuperServe bootstrap provisioned in `onSession` (template baking gap)
- Reference subagents on OpenRouter (not Vercel Gateway strings)

## Deployed model evals

`npm run eval:deployed:flagship` hits **Vercel AI Gateway free-tier rate limits** on production. Session smoke passes; full model evals need paid gateway credits.

## CI secrets (GitHub)

- `OPENROUTER_API_KEY`, `SUPERSERVE_API_KEY`, `VERCEL_TOKEN`, `VERCEL_ORG_ID`
- `VERCEL_PROJECT_ID_FLAGSHIP`, `VERCEL_PROJECT_ID_SUPPORT`, `MONID_API_KEY`
- Vars: `EVAL_S_TIER_ENABLED`, `VERCEL_DEPLOY_ENABLED`, `EVAL_ROTATE_ENABLED` = true